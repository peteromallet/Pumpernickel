# S2a — Insert Sites Registry

Auto-generated by pre-flight grep sweep (T1). Every `INSERT INTO` under `app/` against a table relevant to S2a stamping is catalogued below. Schema filter: `table_schema = 'mediator'`.

---

## Scope-Stamp Tables (require `bot_id`/`topic_id` in S2a)

### `messages`

| # | File | Line | Direction | Will Stamp |
|---|---|---|---|---|
| 1 | `app/services/inbound.py` | 161 | inbound | `bot_id`, `topic_id` (T5) |
| 2 | `app/services/messaging.py` | 55 | outbound (with bot_turn_id) | `bot_id`, `topic_id` (T7) |
| 3 | `app/services/messaging.py` | 84 | outbound (simple) | `bot_id`, `topic_id` (T7) |

### `bot_turns`

| # | File | Line | Will Stamp |
|---|---|---|---|
| 4 | `app/services/agentic.py` | 483 | `bot_id`, `topic_id`, `bot_spec_version`, `hot_context_builder_version`, `tool_schema_version` (T6) |

### `scheduled_jobs`

| # | File | Line | Context | Will Stamp |
|---|---|---|---|---|
| 5 | `app/services/agentic.py` | 577 | deferred_turn | `bot_id`, `topic_id` (T6) |
| 6 | `app/services/scheduled_jobs.py` | 216 | heartbeat seed | `bot_id`='mediator', `topic_id` (T8) |
| 7 | `app/services/checkins.py` | 37 | checkin seed | `bot_id`, `topic_id` (T8) |
| 8 | `app/services/scheduled_job_handlers.py` | 200 | recurring scheduled_task reschedule | read `job['bot_id']`/`job['topic_id']`, re-stamp (T8) |
| 9 | `app/services/scheduled_job_handlers.py` | 318 | weekly summary seed/reschedule | read `job['bot_id']`/`job['topic_id']`, re-stamp (T8) |
| 10 | `app/services/tools/write_tools.py` | 183 | `_schedule_context_job` helper | `bot_id`, `topic_id` kwargs from callers (T8) |
| 11 | `app/services/tools/write_tools.py` | 1367 | `schedule_task` | `ctx.bot_id`, `ctx.primary_topic_id` (T8) |

### `feedback`

| # | File | Line | Context | Will Stamp |
|---|---|---|---|---|
| 12 | `app/services/inbound.py` | 105 | WhatsApp reaction handler | `bot_id`, `topic_id` (T5) |
| 13 | `app/services/discord.py` | 470 | Discord reaction handler | `bot_id`, `topic_id` via `discord_bot_user_id()` scope resolution (T10) |
| 14 | `app/services/tools/write_tools.py` | 1763 | `log_feedback` tool | `bot_id`=`ctx.bot_id`, `topic_id`=`ctx.primary_topic_id` (T10) |

### `bridge_candidates`

| # | File | Line | Will Stamp |
|---|---|---|---|
| 15 | `app/services/tools/write_tools.py` | 305 | `bot_id`=`ctx.bot_id`, `topic_id`=`ctx.primary_topic_id`, `dyad_id`=`ctx.dyad_id` (NOT `ctx.binding_id`) (T10) |

---

## Artifact Tables (require `recorded_by_bot_id` + `artifact_topics` row in same transaction)

### `memories`

| # | File | Line | Type | Will Change |
|---|---|---|---|---|
| 16 | `app/services/tools/write_tools.py` | 699 | Simple INSERT | Single-CTE: `INSERT INTO memories (..., recorded_by_bot_id) ...; INSERT INTO artifact_topics ...` (T9) |
| 17 | `app/services/tools/write_tools.py` | 746 | `supersede_memory` (multi-arm CTE) | EXTEND with `topic_link` arm — never rewrite existing arms (T9) |

### `themes`

| # | File | Line | Type | Will Change |
|---|---|---|---|---|
| 18 | `app/services/tools/write_tools.py` | 764 | Simple INSERT | Single-CTE with `artifact_topics` companion (T9) |

### `watch_items`

| # | File | Line | Type | Will Change |
|---|---|---|---|---|
| 19 | `app/services/tools/write_tools.py` | 800 | Simple INSERT | Single-CTE with `artifact_topics` companion (T9) |

### `observations`

| # | File | Line | Type | Will Change |
|---|---|---|---|---|
| 20 | `app/services/tools/write_tools.py` | 878 | Simple INSERT | Single-CTE with `artifact_topics` companion (T9) |

### `distillations`

| # | File | Line | Type | Will Change |
|---|---|---|---|---|
| 21 | `app/services/tools/write_tools.py` | 939 | Simple INSERT | Single-CTE with `artifact_topics` companion (T9) |
| 22 | `app/services/tools/write_tools.py` | 1049 | `revise_distillation` (multi-arm CTE) | EXTEND with `topic_link` arm — never rewrite existing arms (T9) |

### `out_of_bounds`

| # | File | Line | Type | Will Change |
|---|---|---|---|---|
| 23 | `app/services/tools/write_tools.py` | 1109 | Simple INSERT | Single-CTE with `artifact_topics` companion (T9) |

---

## Non-Stamp Tables (out of S2a scope)

| # | File | Line | Table | Note |
|---|---|---|---|---|
| 24 | `app/services/tools/write_tools.py` | 136 | `tool_calls` | No scope columns — not stamped |
| 25 | `app/services/withheld_reviews.py` | 26 | `withheld_outbound_reviews` | Kwarg pass-through only; no column change in S2a |
| 26 | `app/models/user.py` | 155 | `users` | Out of scope |
| 27 | `app/models/user.py` | 212 | `pacing_events` | Out of scope |
| 28 | `app/services/turn_audit.py` | 88 | `turn_audit_events` | Observability metadata only (T14) |
| 29 | `app/services/system_state.py` | 32,48 | `system_state` | Out of scope |
| 30 | `app/services/spend.py` | 27 | `llm_spend_log` | Out of scope |

---

## Verification Notes

- Line numbers may drift by ±2 across edits. Functional anchors (function names, SQL patterns) take precedence.
- All `information_schema` queries used `table_schema = 'mediator'` filter (S1 lesson #5).
- The `messaging.py:55` and `:84` INSERTs represent the two code paths in `_insert_outbound`: one with `bot_turn_id`/`outbound_part_key` columns, one without.
- `scheduled_job_handlers.py` re-stamping (sites 8 and 9) read `job['bot_id']`/`job['topic_id']` from the originating row; mediator defaults used if absent (legacy boundary).