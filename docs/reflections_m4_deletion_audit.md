# Reflections M4 Deletion Audit

> **Plan:** `m4-product-hardening-and-ship-20260720-0926`
> **Batch:** `14` (`T14`)
> **Generated:** `2026-07-20`
> **Status key:** `cleaned` | `not_applicable` | `blocked` | `deferred`

## Summary

- `cleanup_deleted_reflection_state()` now runs from the explicit delete paths in `app/services/inbound.py`, `app/services/discord.py`, and `app/services/tools/write_tools.py`, and from the 24-hour purge path in `app/services/deletion.py`.
- Reflection-session, reflection-entry, reflection-derivation, embedding, search, tool-read, reflection-admin, hot-context, and normalization-bridge surfaces are cleaned or reduced to audit-safe tombstones.
- No unresolved deleted-reflection visibility remains on the previously blocked derived-target admin or Compass surfaces.
- Any future `blocked` or `deferred` row below with unresolved visibility on a found surface is `staging-blocking`. As of `2026-07-20`, this audit has no remaining staging-blocking rows.

## Surface Matrix

| # | Checked Surface | Checked Files | Cleanup Action | Test Coverage | Remaining Visibility | Status | Staging |
|---|-----------------|---------------|----------------|---------------|----------------------|--------|---------|
| 1 | Source-message delete ingress and 24h purge orchestration | `app/services/inbound.py`, `app/services/discord.py`, `app/services/tools/write_tools.py`, `app/services/deletion.py` | All known delete paths call `cleanup_deleted_reflection_state()`. `purge_expired_deletions()` runs reflection cleanup before rewriting message content to `[deleted]`. | `tests/test_reflection_deletion_cleanup.py` (`test_purge_expired_deletions_runs_reflection_cleanup_before_message_rewrite`) | Source messages persist only as normal message tombstones after the existing message-deletion rewrite. | `cleaned` | `not_blocking` |
| 2 | Reflection sessions | `app/services/deletion.py` | Matching `mediator.reflection_sessions` rows are claim-cleared and moved to `abandoned` unless already `processed`; failure fields are nulled or replaced with `source_message_deleted` audit text. | `tests/test_reflection_deletion_cleanup.py` (`test_cleanup_deleted_reflection_state_updates_all_surfaces`) | Session rows remain in-place as audit-safe tombstones; deleted-source sessions are hidden from visibility-gated reads. | `cleaned` | `not_blocking` |
| 3 | Reflection entries and plaintext-searchable retention | `app/services/deletion.py`, `app/services/reflections.py` | Matching `mediator.reflection_entries` rows have `payload_encrypted`, `summary_encrypted`, and `plaintext_searchable` set to `NULL`. Visible-entry queries now require live source messages and processed sessions. | `tests/test_reflection_deletion_cleanup.py` (`test_cleanup_deleted_reflection_state_updates_all_surfaces`, `test_store_visible_entry_query_requires_live_processed_source_messages`) | Entry rows remain only as schema-required tombstones with searchable/user-facing plaintext removed. | `cleaned` | `not_blocking` |
| 4 | Reflection derivations | `app/services/deletion.py` | Matching `mediator.reflection_derivations` rows have candidate payload cleared, support ids emptied, eligibility rewritten to `["source_message_deleted"]`, and decision forced to `superseded`. | `tests/test_reflection_deletion_cleanup.py` (`test_cleanup_deleted_reflection_state_updates_all_surfaces`) | Derivation rows remain as audit records without candidate payload or live support links. | `cleaned` | `not_blocking` |
| 5 | Reflection search / embedding state | `app/services/deletion.py`, `app/services/reflections.py` | Reflection-entry and derived-target embeddings are deleted from `mediator.content_embeddings`; matching `mediator.embed_jobs` rows are cancelled. Clearing `plaintext_searchable` removes reflection-entry search text as well. | `tests/test_reflection_deletion_cleanup.py` (`test_cleanup_deleted_reflection_state_updates_all_surfaces`) | No separate reflection-specific search index remains after cleanup; only base tombstone rows persist. | `cleaned` | `not_blocking` |
| 6 | Reflection tool reads and search results | `app/services/tools/reflection_tools.py`, `app/services/reflections.py` | `list_reflections()`, `get_reflection()`, correction reconciliation reads, and related entry lookups all use `visible_only=True`; visible queries now require non-deleted source messages plus non-empty `plaintext_searchable`. | Code-path coverage from `tests/test_reflection_deletion_cleanup.py` (`test_store_visible_entry_query_requires_live_processed_source_messages`) and prior reflection-tool coverage in `tests/test_reflection_tools.py` | Deleted-source reflection entries stop being returned through the reflection tool surface. | `cleaned` | `not_blocking` |
| 7 | Reflection-specific admin listing | `app/services/reflections.py`, `app/routers/admin.py` | `admin_list_sessions()` now appends `_session_source_alive_condition("rs")`, excluding sessions whose opener or any source message has been deleted. | `tests/test_reflection_deletion_cleanup.py` (`test_admin_list_sessions_excludes_deleted_source_sessions`) plus prior admin coverage in `tests/test_admin_reflections.py` | Deleted-source reflection sessions are hidden from `/admin/reflections`; only DB tombstones remain. | `cleaned` | `not_blocking` |
| 8 | Hot-context reflection digest | `app/services/hot_context_solo.py`, `app/services/deletion.py` | Digest query already requires processed sessions and non-empty `plaintext_searchable`; cleanup now nulls `plaintext_searchable`, which suppresses deleted-source entries from the digest. | `tests/test_reflection_deletion_cleanup.py` (`test_cleanup_deleted_reflection_state_updates_all_surfaces`, `test_hot_context_reflection_digest_requires_embeddable_live_entries`) plus existing digest coverage in `tests/test_hot_context_reflections.py` | No deleted-source reflection text should remain in the reflection digest once cleanup runs. | `cleaned` | `not_blocking` |
| 9 | Normalization bridge source hydration | `app/services/reflections_normalization_bridge.py` | `_fetch_message_texts()` already filters `messages.deleted_at IS NULL`, so deleted source messages stop contributing raw text to future normalization. | `tests/test_reflection_deletion_cleanup.py` (`test_normalization_bridge_excludes_deleted_source_messages`) | No unresolved visibility observed on this surface after combining the existing `deleted_at IS NULL` filter with the new cleanup helper. | `cleaned` | `not_blocking` |
| 10 | Derived memories | `app/services/deletion.py`, `app/routers/admin.py` | Cleanup demotes derived `memories` rows from `active` to `invalidated` and deletes their embeddings. Admin rendering now excludes `invalidated` rows before user/status filtering. | `tests/test_reflection_deletion_cleanup.py` (`test_cleanup_deleted_reflection_state_updates_all_surfaces`, `test_admin_memories_excludes_invalidated_reflection_tombstones`, `test_admin_memories_status_filter_does_not_resurrect_invalidated_tombstones`) | Deleted reflection-derived memory content no longer renders on `/admin/memories`; only non-content tombstone state remains in storage. | `cleaned` | `not_blocking` |
| 11 | Derived observations | `app/services/deletion.py`, `app/routers/admin.py` | Cleanup demotes derived `observations` rows from `active` to `stale` and deletes their embeddings. Admin rendering now requires `status='active'`. | `tests/test_reflection_deletion_cleanup.py` (`test_cleanup_deleted_reflection_state_updates_all_surfaces`, `test_admin_observations_excludes_stale_reflection_tombstones`) | Deleted reflection-derived observation content no longer renders on `/admin/observations`; only non-content tombstone state remains in storage. | `cleaned` | `not_blocking` |
| 12 | Derived distillations | `app/services/deletion.py`, `app/routers/admin.py` | Cleanup demotes derived `distillations` rows from `active` to `invalidated` and deletes their embeddings. Admin rendering now excludes `invalidated` rows before status/search filtering. | `tests/test_reflection_deletion_cleanup.py` (`test_cleanup_deleted_reflection_state_updates_all_surfaces`, `test_admin_distillations_excludes_invalidated_reflection_tombstones`, `test_admin_distillations_status_filter_does_not_resurrect_invalidated_tombstones`) | Deleted reflection-derived distillation content and shareable summaries no longer render on `/admin/distillations`; only non-content tombstone state remains in storage. | `cleaned` | `not_blocking` |
| 13 | Derived user-orientation items / Compass | `app/services/deletion.py`, `app/services/user_orientation.py`, `app/services/compass.py` | Cleanup retires derived `mediator.user_orientation_items` rows and stamps `closed_reason='source_message_deleted'`. Compass visibility now treats that closed reason as hidden. | `tests/test_reflection_deletion_cleanup.py` (`test_cleanup_deleted_reflection_state_updates_all_surfaces`, `test_compass_visibility_hides_source_message_deleted_tombstones`) | Deleted reflection-derived orientation content no longer reaches Compass or downstream hot context. | `cleaned` | `not_blocking` |
| 14 | Separate reflection-specific search index or extra retention worker | Repository-wide audit from `T1`/`T12`, `app/services/deletion.py` | No standalone reflection-only search index or second reflection-retention worker exists beyond `plaintext_searchable`, `mediator.content_embeddings`, and the existing message purge job already covered above. | N/A | No additional surface exists to clean. | `not_applicable` | `not_blocking` |

## Verification Evidence

- `python -m pytest tests/test_reflection_deletion_cleanup.py -v --tb=short`
  - `12 passed in 0.07s` on `2026-07-20`
- Prior supporting evidence from earlier M4 batches:
  - `tests/test_admin_reflections.py` covers `/admin/reflections` field exposure and redaction-safe output.
  - `tests/test_reflection_tools.py` covers the reflection tool read/search surfaces that now rely on `visible_only=True`.
  - `tests/test_hot_context_reflections.py` covers reflection digest rendering behavior; deletion cleanup now removes the digest input by nulling `plaintext_searchable`.

## Blocking Findings For Staging

None currently identified in this deletion-specific audit.

## Deferred / Residual Notes

- There are no `deferred` rows in the current audit.
- If any future deletion surface is discovered and its visibility remains unresolved, it must be added here as `blocked` or `deferred`; either state is `staging-blocking` for found surfaces until visibility is removed or reduced to an audit-safe tombstone.
